import React from 'react';
import { Text } from 'react-native';
import KMFont from '../hooks/useFont.hook';
// imports ////////////////////////////////

// react function /////////////////////////
export default function SettingNav() {
  // local hooks:

  // local handlers:

  // local ui:
  return <Text style={{ fontFamily: KMFont.Regular }}>الاعدادات</Text>;
}
